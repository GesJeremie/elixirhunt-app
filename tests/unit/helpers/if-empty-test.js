import { ifEmpty } from 'elixircave/helpers/if-empty';
import { module, test } from 'qunit';

module('Unit | Helper | if empty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifEmpty([42]);
  assert.ok(result);
});
