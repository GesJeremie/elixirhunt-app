import { humanizeDate } from 'elixircave/helpers/humanize-date';
import { module, test } from 'qunit';

module('Unit | Helper | humanize date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanizeDate([42]);
  assert.ok(result);
});
