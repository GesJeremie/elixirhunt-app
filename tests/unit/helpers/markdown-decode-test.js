import { markdownDecode } from 'elixircave/helpers/markdown-decode';
import { module, test } from 'qunit';

module('Unit | Helper | markdown decode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = markdownDecode([42]);
  assert.ok(result);
});
